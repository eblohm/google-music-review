import styled from 'styled-components';

const MusicStyles = styled.li`
  list-style-type: none;

  .music--name {
    font-size: 3rem;
  }
`;

const SectionStyles = styled.section``;

const SectionHeader = styled.h2``;

export { MusicStyles, SectionStyles, SectionHeader };
